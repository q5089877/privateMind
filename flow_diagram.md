# 思緒停靠（Mind Harbor）流程架構：首頁微溫停靠版 (V8.2 - 純陳述無債務版)

這是結合「零時間壓迫」與「AI 即時映照」的最終流程全貌。保留了 AI 的核心接住能力，但把後續的探索主控權完全交還給使用者，並徹底根除「回應債務」。

```text
開啟 App
   │
   ▼
HOME 畫面渲染
   │
   ├─ 背景檢查：有需要承接的未完思緒嗎？(getContinuityCandidate)
   │     (條件: 跨日 + 未顯示過 + !carrySuppressed + 長度≥4字)
   │
   └─ 若有 → 首頁底部顯示極淡的可選入口：「昨天有一句話還留在這裡。」
        │
        ├─ 點擊入口展開 → 顯示狀態確認：「昨天留下的這件事。」
        │    ├─ 點擊 [還在]   → carryState: 'still' 
        │    │                  瞬間跳轉進入 CHAT 畫面（歷史對話空間），還原當時情境，
        │    │                  讓使用者可以順著這句繼續往下說（無縫承接）。
        │    ├─ 點擊 [淡掉了] → carryState: 'faded' (狀態確認立即消失)
        │    └─ 點擊 [先不提] → carrySuppressed: true (進入沉默期，短期不再浮現)
        └─ (不點擊，直接去寫字) → 僅標記 shownAt，不寫入任何心理狀態

═══════════════════════════════════════════════════════════════
【路徑 A：不說 (純定錨)】

長按右上角 [⚓ 定錨] 按鈕
   │
   ▼
全螢幕注水 + 生理心跳 Haptic (~60BPM) ➔ 放開手指 ➔ 2000ms 退潮 ➔ 顯示極輕文字：「好。」

═══════════════════════════════════════════════════════════════
【路徑 B：單次停靠與微溫接住 (首頁)】

在輸入框寫下 Moment (起點) → 點擊「留下這句」
   │
   ▼
寫入 DB (發動 MOMENT_DOCKED) ＋ 輸入框清空
畫面保持在 HOME，背景非同步觸發 Present Companion
   │
   ▼
首頁下方浮現「微溫停靠卡片」：
┌──────────────────────────────────────────────┐
│ (微光提示：已安放)                                            │
│                                                              │
│ [AI 純陳述映照] 書借回來了，但沒有翻開。這中間卡住的，       │
│ 是單純沒有體力，還是其實心裡沒有那麼想看。                     │
│ (※ 嚴禁問號，以句號收尾，不要求表態)                          │
│                                                              │
│ 順著這句往下寫   換個角度看看  (低對比文字連結)                │
└──────────────────────────────────────────────┘
   │
   ├─ 【不點擊，直接放下手機 / 離開】
   │    卡片 5-6 秒後「自然淡出」，不留清理債務。
   │    使用者獲得了 AI 的理解，沒有任何必須回覆的壓力。
   │
   ├─ 【手指觸碰卡片 / 滾動】
   │    中斷淡出計時，讓使用者有充裕時間決定是否進入深潛。
   │
   └─ 【點擊文字連結】
        進入 路徑 C 延伸對話。

═══════════════════════════════════════════════════════════════
【路徑 C：延伸對話 (自主選擇深潛)】

從「微溫停靠卡片」點擊連結，畫面轉入 CHAT
   │
   ├─ 點擊 [順著這句往下寫] ➔ 使用者輸入第二句 (形成 Session 對話串)
   │
   └─ 點擊 [換個角度看看]   ➔ 呼叫 Explore Companion
         │
         ▼
      生成 3~4 張「正交認知軸度」視角卡 (事實/時間/控制/身體等)
         │
         ▼
      覺得差不多了，點擊 [今天先到這裡]
         │
         ▼
      LAND 收束畫面 (Landing Companion 產生「思緒邊界草稿」)
         ├─ 區分「這次先帶走」與「留在明天看」
         └─ 點擊 [安放並回到現在] ➔ 完成封箱，回到 HOME

═══════════════════════════════════════════════════════════════
【路徑 D：回看與時間線 (REVIEW)】

從首頁點擊「回看以前留下的事」➔ 進入 REVIEW 時間線
核心原則：AI 只是腳手架，使用者的原文與收束結論才是主角。
   │
   ├─ Pattern 靜默鏡映 (若達門檻)
   │    └─ 顯示微提示：「這幾件事，好像在碰同一個地方。」
   │
   └─ 時間線列表 (依時間倒序排列)
        │
        ├─ 單點 Moment (未深潛過)
        │    └─ 只顯示該句話與時間。
        │
        └─ Session 對話串 (有接續寫下第二句以上)
             呈現「三明治結構」以降低視覺噪音：
             ┌────────────────────────────────────┐
             │ ❶ 起點：借來的書很多都沒看                         │
             │     ··· (隱藏的對話與 AI 映照)               │
             │ ❷ 終點 (若有收束)：[這次先帶走] 發現對厚書有壓力      │
             └────────────────────────────────────┘
             │
             └─ 點擊卡片 ➔ 完整展開還原現場
```
