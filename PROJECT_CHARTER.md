# 思緒停靠 (Mind Harbor) — 產品憲法 V7.1

> **「思緒停靠不替你整理人生。它只把腦中的東西接住，讓它先有一個地方存在；你可以繼續說，也可以先放著。想回來時，再把它帶回來。」**

---

# 1. 產品最終定位

思緒停靠不是 Todo App，也不是日記 App，更不是心理治療工具。

它是一個：
> **讓當下腦中的內容快速離開工作記憶，並在時間中保留脈絡的個人思緒空間。**

承載三種內容：
1. **念頭**（如：「爸媽年紀越來越大了。」）
2. **感受**（如：「今天突然很委屈。」）
3. **行動意圖**（如：「明天問朋友有沒有推薦的搬家公司。」）

入口不要求分類。先讓它進來，再由時間與使用者自己的後續行動形成脈絡。

---

# 2. 核心原則：入口不分類，承載有物理感

* **2.1 入口零分類**：首頁唯一問題只有「現在腦中有什麼？」。不問念頭還是待辦、不問情緒還是事件、不問下一步。先接住。
* **2.2 啟動即輸入**：開啟 App 即自動 Focus 輸入框並喚起鍵盤，打開就是可以說話的時候。

---

# 3. 寫入儀式：讓文字「落下」

使用者輸入文字按下送出：
```text
輸入 → 文字離開輸入框 → 輕輕落下 → 停在時間線 → 「已停靠。」
```
* **「已停靠。」**：只代表這句話已經有地方存在了。不代表放下、不代表解決、不代表情緒已經好了。

---

# 4. 寫入後：兩個自然出口

* **「到這裡就好」** $\rightarrow$ 「我先說到這裡」，回到首頁，不要求使用者得到結論。
* **「＋ 接著說……」** $\rightarrow$ 「我還有一些想說」，展開輸入框，新文字成為新時間節點，不修改前面內容。

---

# 5. 時間線：讓變化自己留下來

* 過去不需要被覆寫：昨天的想法是真的，今天的想法也是真的。
* 時間線保存的不是「正確答案」，而是「你曾經怎麼想，以及後來怎麼變」。

---

# 6. 封存儀式：把它移出眼前

當使用者暫時不想再看這個 Thread：不是「完成」，不是「放下」，而是 **封存**。

### 拖拉物理互動：
* 按住 Thread 向下拖曳 $\rightarrow$ 接近封存區時卡片逐漸下沉 $\rightarrow$ 到達位置慢慢沉下去 $\rightarrow$ **「已封存」** $\rightarrow$ 回到首頁。
* **封存的心理語意**：「我現在不想再看它了」，而不是「我已經解決它了」。

---

# 7. 封存不是刪除

| 操作 | 意義 | 資料層 |
| :--- | :--- | :--- |
| **封存 (Archive)** | 暫時移出眼前主要視線 | 內容完整存在，標記 `isArchived: true` |
| **還原 (Restore)** | 把它帶回原本的思緒空間 | 回到原本的 Thread 與時間脈絡，不建副本 |
| **刪除 (Delete)** | 我真的不要這筆紀錄了 | 本機永久抹除（不可逆） |

---

# 8. 回來：把它帶回來

* 首頁點擊「回來看看」進入時間線，分為：**【正在這裡的】** 與 **【已封存的】**。
* 在已封存 Thread 中點擊 **`↥ 帶回來`**：
```text
封存 → Thread 慢慢浮回 → 回到原本的時間線 → 「已回來。」
```
* 零說教反饋：不出現「恭喜你重新面對它」或「歡迎回來」，不替使用者解釋心理意義。

---

# 9. 刪除：獨立分層

刪除不能和封存放在同一個主要操作層級。
```text
Thread → 更多 (⋯) → 刪除 → 確認刪除頁 → 取消 / 永久刪除 → 本機資料抹除
```
* 確認文案：「確定要刪除這段紀錄嗎？刪除後將無法還原。」

---

# 10. 儀式感的五個物理語言

| 行為 | 物理隱喻 |
| :--- | :--- |
| **寫下** | **「落下」** |
| **繼續** | **「再落下一筆」** |
| **封存** | **「沉下去」** |
| **還原** | **「浮回來」** |
| **刪除** | **「消失」** |

---

# 11. 儀式感鐵律

* **可以有**：輕微沉降、空白、阻尼、淡入淡出、拖拉、回彈、時間流動、很短的狀態文字。
* **嚴禁出現**：🌟 恭喜、🎉 完成、連續天數、分數、心情改善%、任務完成率、紅色逾期、AI 替你解釋情緒。
* **原則**：儀式感服務的是「轉換」，不是「評價」。

---

# 12. 極簡資料模型 (Data Schema)

```typescript
export type EntryType = 'thought' | 'action';

export interface DialogueEntry {
  id: string;
  threadId?: string;
  content: string;
  createdAt: number;
  type?: EntryType;
}

export interface ThoughtThread {
  id: string;
  createdAt: number;
  updatedAt: number;
  isArchived?: boolean; // 改變是否出現在主要視線，非心理結論
  currentActionId?: string | null;
  entries: DialogueEntry[];
}
```

---

# 13. 一句話定義 V7.1

> **思緒停靠不是讓你「完成」思緒，而是讓思緒有地方落下；想繼續，就再說一點；暫時不想看，就封存；有一天想回來，就把它帶回來。**
