# 思緒停靠 (Mind Harbor) — 產品憲法 (Product Charter v6)

> **「思緒停靠不是待辦工具，也不是心理分析工具。它是一個讓人把『此刻腦中存在的東西』暫時放下來，並讓它隨時間留下痕跡的空間。」**

---

# 1. 核心定位與最高原則

使用者不需要先判斷：
* 這是情緒嗎？
* 這是問題嗎？
* 這是待辦嗎？
* 我要不要處理？
* 我應該怎麼想？
* 我現在是不是應該放下？

**想到，就留下。**

### 核心底層哲學：
1. **想到，就留下；想說，再回來；不必現在處理。**
2. **思緒停靠不負責讓事情變好。它只負責讓你不必一直把它握在腦中。**
3. **「先放這裡」不代表放下；「接著說」不代表繼續處理。它們只是介面動作。**

---

# 2. 唯一入口與零阻力互動原則

> **「進入思緒停靠，不是進入『看首頁』；而是直接進入『把現在腦中的東西留下來』。」**

1. **啟動即輸入**：App 啟動後，輸入框自動取得焦點並喚起系統鍵盤。使用者無需先點擊輸入區，即可直接留下文字。
2. **唯一最高優先級**：輸入是唯一的第一優先行為；瀏覽、回顧與其他功能不應在進入瞬間與它競爭注意力。

首頁只問：
> **現在腦中有什麼？**

提供少量情緒快選（鍵盤升起時依然可見、可點）：
> **生氣　委屈　焦慮　難過　害怕　開心　說不上來**

* **快選不是分類**：點選「委屈」，留下的就是客觀字詞「委屈」。
* **零多餘解釋**：不產生任何心理分析、不探索情緒成因。
* **次要入口**：「回來看看」置於最底部次要層次，不搶佔焦點。

---

# 3. 輸入之後

按下送出，畫面落下：
> **已停靠。**

然後回到安靜狀態。
* 不問「為什麼？」
* 不問「你想怎麼處理？」
* 不問「要不要制定行動？」
* 不要求「再寫三句。」

---

# 4. 時間線是產品真正的結構 (Thread + Entry + 時間)

不是資料夾、不是標籤、不是分類、不是狀態，而是 **時間**。

```text
8/27
爸媽年紀越來越大了。
   ↓
8/29
好像真的應該多陪他們吃飯。
   ↓
9/03
昨天陪爸媽吃飯，突然覺得其實我只是害怕有一天來不及。
   ↓
9/18
最近比較能接受這件事了。
   ↓
＋ 接著說……
```

中間沒有覆寫、完成、未完成、最新版本、正確答案、狀態更新。
同一個人在不同時間，留下不同的自己。

### 不神化時間：
* 嚴禁宣稱「時間會治癒你」。
* 嚴禁宣稱「回頭看就會產生覺察」。
* 系統只負責：**時間會留下變化**。至於使用者最後看見什麼，由使用者自己決定。

---

# 5. 待辦邊界 (No Todo Management)

使用者寫下「明天記得買牛奶」：
* 思緒停靠只負責：**讓「明天記得買牛奶」離開腦袋，暫時存在這裡。**
* 思緒停靠不負責將其轉化為待辦清單，不提供提醒、到期時間、Checkbox 或優先級。
* **邊界**：我們根本不接管你的待辦。

---

# 6. Thread 裡的 3 個介面動作

系統對紀錄只提供 3 種客觀介面動作，不賦予任何心理意義：

| 操作 | 介面行為 | 系統意義 |
| :--- | :--- | :--- |
| **＋ 接著說……** | 追加一筆時間節點 | 我又想到一些（不覆寫、純追加） |
| **🍃 先放這裡** | 離開目前畫面，回到安靜首頁 | 純呈現操作，紀錄仍在時間線中，無狀態變更 |
| **🗑 刪除** | 本機永久移除此筆紀錄 | 純資料操作，非心理處理 |

---

# 7. 極簡資料模型 (Data Schema)

唯一保留的結構：**Thread + Entry + 時間**。

```typescript
export interface DialogueEntry {
  id: string;
  threadId?: string;
  content: string;
  createdAt: number;
}

export interface ThoughtThread {
  id: string;
  createdAt: number;
  updatedAt: number;
  entries: DialogueEntry[];
}
```

* 絕不包含：`isTodo`、`isCompleted`、`isResolved`、`isParked`、`isReleased`、`priority`、`dueDate`、`emotionType`。

---

# 8. 系統憲章禁令

1. **禁止狀態機**：無 Open / In Progress / Done / Parked / Released 狀態轉換。
2. **禁止心理分析與評斷**：不塞入 CBT、薩提爾、佛學、哲學等框架。
3. **禁止待辦侵入**：無勾選、無排程、無推播。
4. **低延遲純粹性**：本地優先（Local-first），寫入即結束。
