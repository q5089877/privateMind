# 思緒停靠 (Mind Harbor) V7.2 — 核心使用流程與產品憲法

> **「思緒停靠不替你整理人生。它只把腦中的東西接住，讓它先有一個地方存在；如果其中有事情想往前走，由你自己決定下一步。」**

---

# 一、主文案與核心原則

## 1. 產品定位

**思緒停靠（Mind Harbor）**不是 Todo App、不是日記，也不是心理治療工具。

它是一個讓你把**此刻腦中的東西先放下來**的空間。

你可以寫：
> 「爸媽年紀越來越大了。」

也可以只是：
> 「明天記得買牛奶。」

或者：
> 「今天突然覺得很委屈。」

系統不要求你先判斷這是「念頭、情緒還是待辦」。

**先寫下來就好。**

---

## 2. 核心原則

### 入口不分類
首頁只問：
> **現在腦中有什麼？**

開啟 App 後，輸入框立即取得焦點並打開手機鍵盤。
不需要先點「新增」、不需要選分類、不需要決定它是什麼。

---

### 時間就是脈絡
每一次寫下的內容，都按照發生的時間留下。舊內容不會被新的內容覆寫。
它們不是三個不同版本，它們是**同一段思緒隨時間留下的痕跡**。

---

### 行動可以存在，但不是 Todo
有些念頭會自然長出一個想做的事情。
如果這已經是你想採取的下一步，可以把它帶成：**當前行動（現在往這裡）**。

當前行動只有一個。它不是待辦清單，沒有逾期、沒有紅點、沒有催促通知。
它只是回答：**「現在，如果我要往前一點，我要做什麼？」**

---

### 封存不是刪除
當一段思緒暫時不想再出現在主要時間線，可以將它**封存**。
封存代表：**「現在先不用看它。」**
封存後可以隨時**還原回原本的時間線**。**必須先封存，移入封存區後才能執行永久刪除。**

---

# 二、AI／提示引擎規格 (V7.2)

### 1. 產品定位
AI／提示引擎不是 V7 的核心，它只是「使用者卡住時可以主動使用的思考入口工具」。
核心即使沒有 AI 也必須完整可用。

### 2. 第一版：本機 Prompt Engine
第一版優先採用：**「本機文字特徵 + Prompt Engine + 通用思考入口庫」**，使用者文字完全保留在裝置本機。

### 3. 七大思考入口庫
1. **【拆開】**：把這件事拆成兩件不同的事 / 把事實和猜測分開 / 把「全部」縮小成一個具體例子。
2. **【具體化】**：找一個最近真的發生的例子 / 把它縮成一個具體瞬間 / 把模糊的地方換成具體描述。
3. **【換角度】**：如果換一個角度看…… / 想一個不同的可能 / 試著替另一個人說一次。
4. **【拉開距離】**：如果今天先不做決定…… / 過幾天再回頭看…… / 如果這不是你的事，而是別人的事……
5. **【聚焦】**：只留下現在最重要的一件事 / 如果只能保留一個問題…… / 把背景先拿掉，只看眼前這一點。
6. **【往前】**：如果現在只往前一點…… / 找一個不用把整件事做完的下一步 / 把事情縮小到現在能做的程度。
7. **【允許不知道】**：也許現在還不用想清楚 / 我現在還不知道的是…… / 先讓這件事待一下。

### 4. 輸出規範
* **每次預設 3 個提示**，抽取自不同維度，近期去重。
* 提供「換一組」重新隨機抽選。
* 點選提示直接帶回輸入框，由使用者自己修改、補充並送出。
* **「也可以不選，直接繼續寫。」**
* 提示內容不自動寫入時間線。

---

# 三、UI 互動文案規範

| 模組 | 觸發 / 狀態 | 介面文案 / 行為 |
| :--- | :--- | :--- |
| **首頁** | 主標題 | **現在腦中有什麼？** |
| | 輸入框 Placeholder | `寫下來就好……` |
| | 情緒快選 | 生氣、委屈、焦慮、難過、害怕、開心、說不上來 |
| | 送出按鈕 | **送出** |
| | 次要入口 | **回來看看** |
| **寫入後** | 送出動效 | 輕微落下、沉降 |
| | 停靠狀態 | **已停靠。** |
| | 主要出口 | **到這裡就好**（回到首頁） |
| | 次要出口 | **＋ 接著說……**（展開輸入，新節點不覆寫） |
| | AI 輔助出口 | **陪我想想**（本機展示 3 個開放思考提示，點擊帶回輸入框） |
| **時間線** | 頂部列整合 | `[ 時間線（N） ]` 與 `[ 封存（N） ]` 整合於 Header，去重複大標題 |
| | 思緒呈現 | 去卡片化純文字垂直流動；時間戳置頂弱化；無雙向聊天氣泡。 |
| | 主要操作 | **＋ 接著說……**（降權為輕量文字連結） |
| | 次要操作選單 | **··· 隱藏選單** $\rightarrow$ `◎ 帶走一小步` / `⌸ 封存`（時間線不提供刪除） |
| | 當前行動展示 | 置頂標註 **`現在往這裡`**，下方保留過去時間線。 |
| **封存區** | 視圖標題 | **已封存的思緒** |
| | 主要操作 | **還原**（浮回主要時間線）與 **刪除**（二度確認 Modal） |
| **刪除** | 刪除前提 | **需先封存才能刪除**（移至封存區後方可執行抹除） |
| | 二度確認 Modal | 標題：`確定要刪除這段紀錄嗎？`<br/>內文：`刪除後，這段內容將從此裝置移除，無法復原。`<br/>選項：`[ 取消 ]` / `[ 刪除 ]` |

---

# 四、極簡資料結構 (TypeScript)

```typescript
export type FlowState = 'HOME' | 'PRESENT_SETTLED' | 'REVIEW';
export type EntryType = 'thought' | 'action';

export interface DialogueEntry {
  id: string;
  threadId?: string;
  content: string;
  createdAt: number;
  type?: EntryType;
}

export interface ThoughtThread {
  id: string;
  createdAt: number;
  updatedAt: number;
  isArchived?: boolean;
  currentActionId?: string | null;
  entries: DialogueEntry[];
}
```

---

# 五、V7.2 核心結論

> **入口只有「寫下來」；時間線負責留下發生過的事；當前行動負責現在要往哪走；封存負責讓它暫時離開眼前；刪除才真正讓它消失；AI 幫你找到下一句，不替你決定下一步。**
