# 思緒停靠 (Mind Harbor) — 產品憲法 (Product Charter V7)

> **「思緒停靠不替你整理人生。它只把腦中的東西接住，讓它先有一個地方存在；如果其中有事情要往前走，由你自己決定下一步。過去留下的痕跡不需要被否定，現在要做的事也不必被遺忘。」**

---

# 1. 產品最終定位

思緒停靠不是 Todo App，也不是日記 App，更不是心理治療工具。

它是一個：
> **讓當下腦中的內容快速離開工作記憶，並在時間中保留脈絡的個人思緒空間。**

承載三種內容：
1. **念頭**（如：「爸媽年紀越來越大了。」）
2. **感受**（如：「今天突然很委屈。」）
3. **行動意圖**（如：「明天問朋友有沒有推薦的搬家公司。」）

三者在呈現上允許合理分化，但入口絕不分類。

---

# 2. 核心原則：入口不分類，出口可分化

* **入口零分類**：寫下當下最重要的是先讓它離開腦中。首頁唯一問題只有「現在腦中有什麼？」。
* **啟動即輸入**：開啟 App 即自動 Focus 輸入框並喚起鍵盤，零點擊阻力。
* **次要入口**：「回來看看」置於首頁底部弱化區域，不爭搶注意力。

---

# 3. 狀態語意邊界與階層化操作

Thread 內的三個按鈕並非「平權」，而是具有完全不同的心理重量與視覺層級：

1. **內容操作（最顯著）**：`＋ 接著說……` $\rightarrow$ 「我還有話要說」，核心內容行為。
2. **離開操作（次要導航）**：`🍃 先放這裡` $\rightarrow$ 「我現在先離開」，純粹導航回首頁，資料 100% 完整保留，不做成綠色完成按鈕。
3. **破壞性操作（邊緣弱化）**：`🗑 刪除` $\rightarrow$ 「我不要這份資料了」，破壞性資料操作，置於最弱邊緣並嚴格二次確認。

---

# 4. 時間線與當前行動 (空間定錨)

### 不可變的是歷史，可變的是現在：
1. **歷史時間線**：普通 Entry 採 Append-only（只增不覆寫），完整保留不同時間的自己。
2. **當前行動（唯一有效版本）**：
   * 當 Thread 中形成具體行動意圖時，上方獨立置頂【當前行動】。
   * 當行動想法改變時，更新當前行動，舊意圖依然留存於歷史時間線。
   * **行動不是 Todo**：無 Checkbox、無逾期提醒、無紅點、無完成率。當前行動只是「你目前認為值得往前走的一個方向」。

---

# 5. 極簡資料模型 (Data Schema)

```typescript
export type EntryType = 'thought' | 'action';

export interface DialogueEntry {
  id: string;
  threadId?: string;
  content: string;
  createdAt: number;
  type?: EntryType; // 'thought' | 'action'
}

export interface ThoughtThread {
  id: string;
  createdAt: number;
  updatedAt: number;
  currentActionId?: string | null; // 指向當前唯一有效行動
  entries: DialogueEntry[];
}
```

* **絕不包含**：`isTodo`、`isCompleted`、`isResolved`、`isParked`、`isReleased`、`priority`、`dueDate`、`emotionType`。

---

# 6. 最終憲章：七條鐵律

1. **01｜接住，不解釋**：使用者說什麼就先讓它存在，系統不替內容尋找原因。
2. **02｜入口零分類**：使用者不需要在輸入當下決定屬性，先寫下來。
3. **03｜歷史不覆寫**：過去的自己不需要被現在修正，每一筆歷史都可以留下。
4. **04｜現在要清楚**：若形成行動意圖，當前行動只有一個；歷史可以很多，現在只需要清楚。
5. **05｜放下不是刪除**：「先放這裡」只是離開，「刪除」才是消失；絕不替使用者宣布放下。
6. **06｜絕不變成 Todo**：無勾選、無逾期、無紅點、無 KPI、無任務壓力。
7. **07｜時間承載意義，但不替意義下結論**：時間線呈現變化，變化代表什麼由使用者自己決定。

---

# 7. 最終一句產品定義

> **思緒停靠，是一個讓腦中的東西先有地方存在的空間；歷史可以留下，現在可以往前，而系統不替你決定它代表什麼。**

* 接住 → 留下 → 讓時間經過 → 必要時往前一步。
* 過去不被覆寫，現在不被混淆，未來不被催促。
