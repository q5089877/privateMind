# 思緒停靠 (Mind Harbor) V7 — 主文案與產品憲法

> **「思緒停靠不替你整理人生。它只把腦中的東西接住，讓它先有一個地方存在；如果其中有事情想往前走，由你自己決定下一步。」**

---

# 一、主文案

## 1. 產品定位

**思緒停靠（Mind Harbor）**不是 Todo App、不是日記，也不是心理治療工具。

它是一個讓你把**此刻腦中的東西先放下來**的空間。

你可以寫：
> 「爸媽年紀越來越大了。」

也可以只是：
> 「明天記得買牛奶。」

或者：
> 「今天突然覺得很委屈。」

系統不要求你先判斷這是「念頭、情緒還是待辦」。

**先寫下來就好。**

---

## 2. 核心原則

### 入口不分類
首頁只問：
> **現在腦中有什麼？**

開啟 App 後，輸入框立即取得焦點並打開手機鍵盤。
不需要先點「新增」。
不需要選分類。
不需要決定它是什麼。

---

### 時間就是脈絡
每一次寫下的內容，都按照發生的時間留下。
舊內容不會被新的內容覆寫。

因此：
> 「爸媽年紀越來越大了。」

之後可以變成：
> 「這週末想陪爸媽吃飯。」

再之後也可以是：
> 「昨天吃完飯，突然發現我其實是害怕有一天來不及。」

它們不是三個不同版本，它們是**同一段思緒隨時間留下的痕跡**。

---

## 3. 行動可以存在，但不是 Todo

有些念頭會自然長出一個想做的事情。

例如：
> 「明天問朋友有沒有推薦的搬家公司。」

如果這已經是你想採取的下一步，可以把它帶成：
> **當前行動**

當前行動只有一個。
它不是待辦清單。
沒有：
* 勾選完成
* 逾期
* 紅點
* 優先級
* 排名
* KPI
* 催促通知

它只是回答：
> **「現在，如果我要往前一點，我要做什麼？」**

如果之後想法改變，可以換成新的當前行動。
**舊的想法仍然留在時間線裡。**

---

## 4. 封存不是刪除

當一段思緒暫時不想再出現在主要時間線，可以將它**封存**。

封存代表：
> **「現在先不用看它。」**

不是：
> 「我已經放下了。」
也不是：
> 「事情已經解決了。」

封存後仍然可以回到封存區，並**還原回原本的時間線**。
真正想讓它消失，才使用「刪除」。

---

# 二、UI 互動文案規範

| 模組 | 觸發 / 狀態 | 介面文案 / 行為 |
| :--- | :--- | :--- |
| **首頁** | 主標題 | **現在腦中有什麼？** |
| | 輸入框 Placeholder | `寫下來就好……` |
| | 情緒快選 | 生氣、委屈、焦慮、難過、害怕、開心、說不上來 |
| | 送出按鈕 | **送出** |
| | 次要入口 | **回來看看** |
| **寫入後** | 送出動效 | 輕微落下、沉降 |
| | 停靠狀態 | **已停靠。** |
| | 主要出口 | **到這裡就好**（回到首頁） |
| | 次要出口 | **＋ 接著說……**（展開輸入，新節點不覆寫） |
| **時間線** | 入口標題與副標 | **時間線** / `時間保留變化，現在指向行動。` |
| | 分流頁籤 | `時間線（N）` 與 `封存（N）` |
| | 思緒呈現 | 垂直安靜流動；時間戳置頂弱化；無雙向聊天氣泡。 |
| | 主要操作 | **＋ 接著說……**（卡片唯一主要操作按鈕） |
| | 次要操作選單 | **⋯ 更多** $\rightarrow$ `設為當前行動` / `封存` / `刪除` |
| | 當前行動展示 | 置頂標註 **`現在往這裡`**，下方保留過去時間線。 |
| **封存區** | 視圖標題 | **已封存的思緒** |
| | 主要操作 | **還原**（浮回主要時間線）與 **⋯ 更多**（`刪除`） |
| **刪除** | 深層選單 | 更多 (⋯) $\rightarrow$ **刪除** |
| | 二度確認 | 標題：`確定要刪除這段紀錄嗎？`<br/>內文：`刪除後，這段內容將從此裝置移除，無法復原。`<br/>選項：`取消` / `刪除` |

---

# 三、極簡資料結構 (TypeScript)

```typescript
export type FlowState = 'HOME' | 'PRESENT_SETTLED' | 'REVIEW';
export type EntryType = 'thought' | 'action';

export interface DialogueEntry {
  id: string;
  threadId?: string;
  content: string;
  createdAt: number;
  type?: EntryType;
}

export interface ThoughtThread {
  id: string;
  createdAt: number;
  updatedAt: number;
  isArchived?: boolean;
  currentActionId?: string | null;
  entries: DialogueEntry[];
}
```

---

# 四、V7 核心結論

> **入口只有「寫下來」；時間線負責留下發生過的事；當前行動負責現在要往哪走；封存負責讓它暫時離開眼前；刪除才真正讓它消失。**

