// The implementation stays in its existing file during the non-visual refactor.
// New flow code imports through this boundary, so storage can later move without UI changes.
export { MindHarborRepository } from '../logic/MindHarborRepository';
